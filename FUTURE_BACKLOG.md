# Future Backlog (Not Part of Official Documents)

## Offline Scope Extension
Current offline acceptance scenarios in `ПМИ` cover only these entities:
- trips;
- ideas;
- itinerary activities;
- expenses.

Planned extension for future iterations:
1. add durable offline queue and sync support for profile updates;
2. add durable offline queue for comment sending (instead of in-memory retry only);
3. define and implement offline behavior for notification settings updates;
4. add dedicated acceptance scenarios in `ПМИ` after implementation.

Status: backlog item, not included in current acceptance scope.
